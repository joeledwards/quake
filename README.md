Fetch earthquake data from the USGS API, parse it, and check it against historically recorded events.

Install Node.js 4+

Install Redis locally

Run `npm install`

Run `npm start` to run the script

